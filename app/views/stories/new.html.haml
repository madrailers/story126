- form_for(@story) do |f|
  /= f.error_messages
  %p
    = f.label :text, "Start your story here"
    %br
    = f.text_area :text, {:rows => 4, :cols => 70, :tabIndex => 0}
  %p
    = f.submit "Create"

= link_to 'Back', stories_path
